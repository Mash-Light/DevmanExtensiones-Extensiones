# Módulos

Cada extensión personalizada debe vivir en una carpeta propia:

```text
modules/<codigo>/
├── specification.yaml
├── README.md
├── src/
├── tests/
└── docs/
```

El código debe coincidir con el identificador interno utilizado por `devman-ext`.
